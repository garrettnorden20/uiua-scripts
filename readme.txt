Uiua scripts

Run these to make it all good.
Seems like I dont need to do it anymore.
Every new file needs the chmod though

cd ~/uiua-scripts
chmod +x *
source ~/.zshrc 2>/dev/null || source ~/.bash_profile 2>/dev/null
hash -r